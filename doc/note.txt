-----------------------July 9----------------------------------

Q, How to control if a tensor need gradient or not.
A, 
    - requires_grad=True/False # 
    - z = x.detach() #推荐
    - z = x.clone().requires_grad_(False) # 不推荐 clone

Q, How to control if computation step need gradient or not.
A, 
    - with torch.no_grad(): / with torch.
    - torch.set_grad_abled(False) / torch.set_grad_abled(True) # 不推荐

Q, Evaluation mode and training mode
A,
    - Evaluation mode:
        - model.eval()
        - used in test or inference
        - close autograd
        - keep all nerual unit alive in Dropout
        - use global mean and global variance in BatchNorm(no-upgrade)


调用关系 __torch_function__ x.sort() torch.sort()
Both x.sort() and torch.sort() will call __torch_function__() with func.__name__ is 'sort'.
x.sort() and torch.sort() do not call each other.

@cacheable
def func(self, x, y)

You should implement this by store cache data on the self entity.

with Cache.enabled:
    func()

---------------July 14---------------------
CI/CD
  - SSH is stabler than https on git.
simple_demo.py
  - Dropout in forward -> self.dropout = Dropout()
    Module的子类都应该在__init__中执行一次初始化，避免在方法中多次调用（避免重复对一个
Module Parameters初始化）

  - super.__init__() is needed in construct function of sub-classes of nn.Module
    Module父类会需要先初始化一些方法的参数 这是必要的
compass-core

  - staticmethod, __new__() is necessary if you define a class inherit torch.Tensor, 
cause normaly you want to implement some different logic from super class

  - **classmethod, __torch_function__() is very important, you should learn more 
details about it.**

Todo list
  - Find out if torch.sort(x) will call x.sort(). If it true, you can only overwrite
sort function in your own Tensor which inherit torch.Tensor.(False)
  - Implement requirement raised in issues #161, #162 and #165
    You should turn to issues' web pages and create a branch for the issue you are watching.
    All PRs(pull requests) will perform pytest auto to check all test function, So you should 
implement both lib script and corresponding test function.
    You also should @ a person to check details of the PR after passed by github's auto 
checkout.  

--------------July 18-------------------
1 配置
shift manual yaml to hydra

2 模型训练时间
模型训练一次 第x天开始用n天
想要知道这次输出距离上次训练有多少天
inference：加注自己的训练时间

3 线代
在约束下拟合一些东西
照着文档结论 把真实数据拟合一下
独立的均值回归-> 联合的均值回归
低秩矩阵（减少计算量 降噪）下的均值回归模型

-----------------------July 25-----------------------------------
use bash $env:PYTHONPATH="C:\Data\compass-core" to set PYTHONPATH


----------------------July 30-------------------------------------
while read requirement; do conda install --yes $requirement || pip install $requirement; done < requirements.txt

git rev-parse HEAD 查看当前分支所处提交的hash

git clean -df 清除多于远程版本的代码

# 测试jasper代码正确性
RUN_LEVEL=SANITY Seeds=seed-00 Data=/data/JP_end_2025-07-22/data_2 python ~/data/compass-app-jasper/app/build.py all 
task build -t ~/data/compass-app-jasper/app/Taskfile.yaml RUN_LEVEL=SANITY Seeds=seed-00 Data=/data/JP_end_2025-07-22/data_2 goal=all ROOT="$(pwd)"

----------------------Aug 8--------------------------------------
ssh peiran@172.16.10.15  - 公司机房机器

C:\azure-cli-2.76.0-x64\bin\az.cmd ssh vm --ip 10.0.2.28 - 虚拟机登录
ssh peiran@fengheasia.com@10.0.2.28 - 虚拟机ssh登录 用户名为peiran@fengheasia.com

---------------------Aug 27---------------------------------------
ssh -Tv git@github.com 测试ssh链接github

# Run override sweep command
python ~/data/compass-app-jasper/app/parent_job.py --data /data/JP_end_2025-04-11/data --sweep-config ~/data/compass-app-jasper/conf/experiments/search_spaces/usecase_rebalance_period.yaml --run-level SANITY

--------------------Sep 2----------------------------------------
tensorboard --bind_all --logdir ./ # Perform in job dir to build a sequence of viewable data which can be seen on web page by creating a service on a default port.
tensorboard --logdir log/seed-00/window_2022-11-28_2024-12-24/loss/ --host 0.0.0.0 #查看matric logdir
http://10.0.2.28:6006/#timeseries # View in web page after perform tensorboard in 10.0.2.28

conda activate finai-1.1.0 # Activate a conda environment which name is finai-1.1.0.


--------------------Sep 3-----------------------------------------
uv run pytest .\test\data\test_extendable_dataframe.py:test_cinsert  

https://fenghe.ai/web2/#/finance-home/index
帐号：peiran@fengheasia.com
密码：N!4rXp&K8vZd

-------------------Sep 4------------------------------------------
# -r 递归拷贝目录
# 从服务器拷贝到本地
scp -r peiran@fengheasia.com@10.0.2.28:~/data/compass-app-jasper/job-0003 C:\Data\Jobs\BACKTEST_2Y,Seed-00\job-0003
# 从本地拷贝到服务器
scp -r /path/to/local_dir username@server_ip:/path/to/remote_dir

# 多任务
python ~/data/compass-app-jasper/app/parent_job.py --data /data/JP_end_2025-07-22/data_2 --run-level BACKTEST_2Y --seeds seed-00
# 两年回测命令 
task build -t ~/data/compass-app-jasper/app/Taskfile.yaml RUN_LEVEL=BACKTEST_2Y Seeds=seed-00 Data=/data/JP_end_2025-07-22/data_2 goal=all ROOT="$(pwd)"

------------------Sep 11--------------------------------------------------
nvidia-smi  #查看显卡有没有被使用


------------------Nov 5----------------------------------------------------
#conda 切换虚拟环境
#屏蔽用户目录环境 cuda programming暂时不能屏蔽用户环境（GPU C++版本过低）
conda activate finai-1.1.0;export PYTHONNOUSERSITE=1
# rabse 前期过渡冗长重复冲突
git checkout --ours .;git add .;git rebase --continue

export PYTHONPATH="/home/peiran/data/compass-app-jasper/app2:/home/peiran/data/compass-app-jasper/core:/home/peiran/data/compass-app-jasper/lib"
export PYTHONPATH="$HOME/data/fenghe-nn/python"